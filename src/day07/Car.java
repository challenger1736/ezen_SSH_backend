package day07;

// ================================= 클래스 용도 : 1.라이브러리 클래스 ==================================/
public class Car {
    
    // 클래스 멤버
    // 1. 필드 : 데이터를 저장하는 곳
        // 필드 선언
        // p.214 : 초기화가 없으면 객체 생성시 기본값으로 초기화 됨.
    String model; // 코드는 있지만 실제 스트링은 존재하지 않는다. 왜 WHY?
    // 실제 실행해서 스레드를 쓰는 순간 존재하게 된다. 이건 설계도일뿐이다.
    boolean start;
    int speed;
    // 아직 존재하지 않음.

    // 2. 생성자 : 객체 생성시 초기화

    // 3. 메소드 : 객체 동작
}
